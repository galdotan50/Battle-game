# משחק הקרבות — קונטקסט לפרויקט

## ⚠️ קודם כול: מי המשתמש

**המשתמש הוא ילד בן 9, ורוב הזמן הוא עובד לבד — בלי אבא שלו.**

לכן:

- **כתוב בעברית, פשוט.** משפטים קצרים. בלי ז'רגון.
- כשאתה חייב להשתמש במונח מקצועי (מש, רינדור, קומיט) — **הסבר אותו במילה-שתיים
  בפעם הראשונה** בכל שיחה. אל תניח שהוא זוכר מאתמול.
- **אל תניח ידע בגיט, בטרמינל או בכלי פיתוח.** אם צריך שהוא יעשה משהו ידנית —
  תן הוראות צעד-אחר-צעד, ובדוק שזה משהו שילד בן 9 יכול לעשות לבד.
- **הוא לא יכול לבצע שלבים באתרים חיצוניים** (העלאת קבצים, פתיחת חשבונות).
  אל תתכנן מסלול שתלוי בזה בלי לומר לו במפורש שצריך עזרה של מבוגר.
- אל תדבר אליו מלמעלה. הוא מבין הרבה — הוא פשוט צריך את זה בשפה שלו.
- **תמיד תגיד את האמת על מה שעובד ומה לא.** אם משהו יצא לא טוב, תגיד. אם משהו
  לא אפשרי, תסביר למה בפשטות ותציע חלופה.
- **⚠️ כלל קבוע (המשתמש ביקש במפורש ב-2026-09-19 שזה יישאר כאן לתמיד):
  כל החלטה טכנית שקשורה לשרת, לדאטהבייס (Firestore/Firebase וכו'),
  לאבטחה, או למשהו "מסובך" באופן כללי (למשל: כללי אבטחה של Firestore,
  Cloud Functions, הפעלת תוכנית תשלום כמו Blaze, שינוי מבנה נתונים
  משותף בין שחקנים) - חובה לערב את אבא שלו לפני שממשיכים, לא להחליט
  לבד ולא רק מול הילד.** תסביר את ההחלטה בפשטות, תציג את החלופות (כולל
  את זו שלא דורשת כרטיס אשראי/הרשאות מיוחדות אם יש כזו), ותחכה לאישור
  מפורש שלו לפני שכותבים קוד שתלוי בהחלטה הזו. זה חל גם אם הילד עצמו
  מבקש להמשיך - השאלה "האם אבא כבר אישר את זה?" תמיד לגיטימית לשאול.

### איך הוא רואה את התוצאה

הוא **מוריד את `index.html` ופותח אותו במחשב בדאבל-קליק**. זה מכתיב מגבלה
טכנית קשיחה — ראה "כללים טכניים" למטה.

### איך לעדכן אותו

**גם תמונות וגם הסבר.** תמיד תשלח צילום מסך של איך זה נראה עכשיו, ולידו משפט
או שניים שמסבירים מה השתנה. תמונה לבד לא מספיקה, והסבר לבד לא מספיק.

---

## מה זה הפרויקט

משחק קרבות תלת-מימדי בדפדפן. השחקן מתחיל עם דמות חלשה (סווארק) ונשק בסיסי,
מנצח בקרבות, מרוויח כסף, קונה נשקים ודמויות חזקות יותר.

- **`game-spec.md`** — מסמך החזון המלא (23 סעיפים: 11 דמויות, נשקים, אוכל,
  משקאות, מגנים עם יכולות מיוחדות, כסף, ציונים, משימות, איוונטים, בוסים).
  **זה מקור האמת למה שהמשחק אמור להיות.**
- **`פיתוח משחק.md`** — יומן המסע: מה נבנה, מה נלמד, ואיפה אנחנו עומדים.
  **קרא אותו בתחילת כל שיחה** — שם כתוב הצעד הבא.
- **`character-v2/README.md`** — תיעוד שיטת בניית הדמויות.

---

## איך עובדים כאן

בונים **שלב אחר שלב**, לא הכל ביחד:

1. כותבים תכנית מדויקת — מה בדיוק ייבנה ואילו החלטות נבחרו.
2. שואלים אם משהו לא ברור.
3. **המשתמש מאשר לפני שנכתב קוד.**
4. בונים, הוא בודק בדפדפן, נותן משוב.
5. מתעדים ביומן, ורק אז עוברים לשלב הבא.

**התובנה הכי חשובה מהפרויקט:** למשתמש קשה לדייק דרישות מטקסט. **הוא צריך
לראות.** בכל שינוי כיוון — תבנה משהו קטן וויזואלי מהר, ותראה לו.

---

## כללים טכניים (קשיחים)

- **קובץ אחד שנפתח בדפדפן.** בלי build step, בלי npm, בלי פריימוורקים.
  three.js נטען מ-CDN דרך importmap.
- **אסור `import` של קובץ מקומי.** דפדפן חוסם ייבוא מודול מ-`file://`. לכן קוד
  הדמויות **מוטמע ישירות ב-`index.html`** ולא בקובץ נפרד. אותה מגבלה חלה על
  טעינת תמונות מקומיות. **`index.html` הוא מקור האמת היחיד לקוד.**
- כל המספרים מהאפיון (כוח, מהירות, נזק, מחירים) הם נתוני פתיחה — לשמור אותם
  במקום אחד כדי שיהיה קל לאזן.
- כשמוסיפים הרבה אובייקטים חוזרים (קהל, קבוצות אויבים) — `InstancedMesh`.

## לקחים שנקנו בדם

- **בגרפיקה תלת-מימדית: לחשב, לא לנחש.** כמעט כל באג ויזואלי בפרויקט נבע
  ממיקום שנקבע בניחוש. פתרו את המשוואה של פני השטח ומקמו לפיה.
- **כל גוף "מקיף"** (קיר, יציע, כיפה) חייב להיבנות כמעטפת חלולה, ותמיד לוודא
  שהמצלמה בחלל הפנימי. זה קרה פעמיים.
- **כדי לאתר באג ויזואלי — תרנדר ותסתכל.** אפשר להתקין three.js מקומית ולצלם
  את הסצנה עם Playwright + Chromium (מותקנים בסביבה). כל הבאגים בדמויות נמצאו
  ככה, לא מקריאת הקוד.
- **אל תטען שמשהו עובד בלי לבדוק.** תרנדר, תסתכל, ורק אז תגיד.
- **ספריות מרובות-קבצים (כמו Firebase) דורשות esbuild, לא רק route
  אחד לקובץ.** three.js הוא קובץ CDN יחיד - `page.route` פשוט מספיק כדי
  להחליף אותו בעותק מקומי (`npm install three` + route אחד). Firebase
  מורכב מכמה קבצי CDN (`firebase-app.js`/`firebase-auth.js`/
  `firebase-firestore.js`) שכולם **חייבים לחלוק את אותו מופע פנימי**
  של `@firebase/app` (אחרת מתקבל `Component auth has not been
  registered yet`) - `npm install firebase` לבד לא מספיק, כי בנייה
  נפרדת של כל קובץ (3 קריאות `esbuild --bundle` נפרדות) מכפילה את
  ה-registry הפנימי בכל קובץ בנפרד. הפתרון: קריאת `esbuild` **אחת**
  עם שלוש נקודות-כניסה ו-`--splitting --format=esm --outdir=...` -
  זה מייצר קובץ chunk משותף אחד שכל שלוש נקודות הכניסה מייבאות ממנו,
  בדיוק כמו הבנייה האמיתית שגוגל מפרסמת ל-CDN. ה-chunk גם צריך route
  נפרד משלו (הדפדפן מבקש אותו בנפרד, לפי import יחסי).

---

## מצב נוכחי

**קיים:** זירת אגרוף תלת-מימדית מלאה (במה, חבלים, קהל, תאורה, מצלמה) +
סווארק ובו, עם שלד אמיתי (Skinning) שמאפשר תנועה חלקה. שליטה מלאה במקלדת:
חיצים לתזוזה, A לאגרוף, S לבעיטה, W למכת אבן חדה, D לחסימה (הידיים נצלבות
ל-X). בו מנוהל על ידי AI בסיסי - מתקרב, שומר מרחק, תוקף (כולל אבן) ולפעמים
חוסם. שני הלוחמים לא יכולים לעבור זה דרך זה, ולא יכולים לצאת מגבולות הזירה.

**חיים ונזק קיימים:** סווארק 100 חיים, בו 75. אגרוף -3, בעיטה -5, אבן חדה -7
(תואם את האפיון). חסימה מבטלת נזק לגמרי. שני פסי חיים למעלה, הבזק אדום
במסך כשמישהו נפגע. כשמישהו מגיע לאפס - מסך ניצחון/הפסד עם שני כפתורים:
"לשחק שוב" (קרב חדש מייד) ו"לובי" (יוצא למסך לובי).

**לובי וחנות קיימים:** מסך לובי עם כפתורי "התחל קרב" ו"חנות", ושתי שורות
חצים (◀▶) - אחת לבחירת איזו דמות **קנויה** לשחק איתה, ואחת לבחירת איזה
**נשק קנוי** לצייד. המצלמה מתקרבת לדמות הנבחרת (בו מוסתר), עם הנשק הנבחר
קבוע ביד. **בחירת דמות משפיעה בפועל**: לוחצים על חץ, `switchPlayerCharacter`
מפרקת את המודל התלת-מימדי הנוכחי של `player` ובונה אחד חדש מהקונפיג של
הדמות שנבחרה (`player` הוא `let`, לא `const`, בדיוק בשביל זה) - ואז נלחמים
איתה בפועל בזירה, עם שם נכון בפס החיים. **בחירת נשק** (`switchPlayerWeapon`)
דומה אבל קלה יותר - רק מחליפה את הנשק על הדמות הקיימת, בלי לבנות אותה
מחדש, ונשמרת גם כשמחליפים דמות. `WEAPON_CFGS` מגדיר **את כל 10 הנשקים
מהחנות** - dmg (נזק אמיתי שמשפיע על הקרב, ראה `tickCombat` למטה) ו-build()
שבונה קבוצת גיאומטריות פרימיטיביות (גליל+חרוט/להב שטוח משולש מוקצע, לא
משטח רציף כמו הדמויות) עם האחיזה בראשית הצירים. `addWeaponProp` מסובבת
ומזיזה כל נשק כך שיתיישר עם כיוון הזרוע ותולה קשיח על עצם שורש כף היד -
בדיוק השיטה שהאבן החדה המקורית השתמשה בה, רק כללית לכל נשק. כרגע כל
הנשקים חולקים את אותה אנימציית מכה וטווח - רק הנזק והמראה משתנים.
כל שורת חצים מנוטרלת בנפרד לפי כמה פריטים בקטגוריה שלה קנויים. החנות
מציגה 6 קטגוריות (דמויות, נשקים, מגנים, אוכל, משקאות, שדרוגים) עם הנתונים
האמיתיים מ-`game-spec.md`. קטגוריית שדרוגים ריקה בכוונה (לא הוגדר באפיון
מה יהיה בה). **קטגוריית הדמויות מציגה תמונות אמיתיות** לכל דמות שכבר
נבנתה בקוד (`CHARACTER_CFGS`) - כרגע סווארק, ג'ני, ברייק, לורד, פלייטי
וליזי - ו"בקרוב" לשאר. התמונות נתפסות בזמן ריצה (`captureCharacterThumb`):
מקטינים את הקנבס לרגע לריבוע קטן, מרנדרים את הדמות, ותופסים תמונה - אפשר
לעשות את זה בלי שיבחינו כי מסך החנות תמיד אטום. תיאור ויזואלי מדויק לכל
11 הדמויות (כולל אלה שעוד לא נבנו) שמור ב-`game-spec.md` סעיף 3.

**כסף וקנייה קיימים:** מוצג תמיד (💰 בפינה) ומתקבל **רק על ניצחון**, 1₪
לכל נקודת נזק שהשחקן גרם באותו קרב, עד תקרה של 52₪ לקרב (`MAX_MONEY_PER_GAME`)
- כדי שקרב אחד לא ייתן יותר מדי. **אפשר לקנות בפועל בחנות** - כפתור על
כל פריט, בודק שיש מספיק כסף, מוריד אותו ומוסיף את הפריט למלאי (`inventory`,
Set פשוט בזיכרון - לא נשמר בין טעינות דף). סווארק והאבן החדה כבר "שייכים"
לשחקן מההתחלה. פריט שכבר נקנה מציג "✅ שלך"; ניסיון קנייה בלי מספיק כסף
מרעיד את הכפתור באדום בלי לקרות שום דבר.

**ג'ני, ברייק, לורד ופלייטי קיימות בקוד** (`JENNY_CFG`, `BRAKE_CFG`,
`LORD_CFG`, `PLAYTY_CFG`) עם יכולות חדשות בבונה הדמויות (נבדק שלא משפיעות
על דמויות קודמות): שכבת "ז'קט" עם פסים או בצבע אחיד ושרוולים אמיתיים
(`jacketMat`+`stripeTex`, עם `cfg.jacketHem` לגובה השכבה על הגוף
ו-`cfg.sleeveLen` לאורך השרוול - ברירת מחדל 0.86 כמעט עד שורש כף היד;
לורד 0.38, פלייטי 0.10 שרוול-כובע קצר מאוד), שיער ארוך (`hairStyle:'long'`),
קצר (`'short'`) או פרוע (`'spiky'`), קשת לשיער (`cfg.hairBow`/
`cfg.hairBowColor`), ריסים+שפתיים צבועות (`cfg.eyelashes`, `cfg.lipColor`),
כתובת שעוטפת בד (`textBandTex`), כיסים (`cfg.pockets`), כפכפים
(`cfg.flipflops`), מכנסי ספורט קצרים (`cfg.shorts`+`cfg.pantsLength`),
מכנס דו-גוני עם פס צד (`cfg.pantsStripeColor`) ומכנס צמוד (`cfg.pantsPad`
קטן מברירת המחדל), סמלי קבוצה/אות גדולה על עיגול קשיח בצבעים
(`cfg.chestCrestTex`/`cfg.pantsLogoTex`, לא עותק מדויק של סמל אמיתי),
קעקוע על הכתף (`cfg.tattooL`/`cfg.tattooR`, עיגול **שקוף** קשיח על עצם
הכתף - ראה הלקח הטכני למטה), וצמיד/טבעת ביד (`cfg.bracelet`/
`cfg.fingerRing`, קיים גם אצל בו). כל הארבע ניתנות לבחירה כ-`player`
דרך חצי הלובי אחרי שנקנות (ראה למעלה). **⚠️ המשפט הבא כאן היה נכון
בזמנו אבל התיישן** - ברטו/סטנבר/סלייקי/קיטי/לוין כן נבנו בפועל בשיחות
מאוחרות יותר (ראה את הסעיף על טיגרו/אסטרו/מגרון/זרקון בהמשך הקובץ,
שמתעד את המצב המלא והעדכני של כל 15 הדמויות ו-12 רמות האויבים).

**ליזי קיימת בקוד** (`LIZZY_CFG`) עם שלוש יכולות חדשות כלליות בבונה
הדמויות (בדוק שלא משפיעות על דמויות קודמות): משקפי ראייה (`cfg.glasses`/
`cfg.glassColor` - מסגרת טורואידית + עדשה שקופה חלקית + זרועות-רקה +
גשר, הכל תלוי קשיח על עצם הראש), נעלי קרוקס (`cfg.crocs`/`cfg.crocColor`
- סוליה עבה + מעטפת בוהן מעוגלת + שלוש "חורים" כדוריים כהים + רצועת עקב,
באותה לולאת-רגל שבונה גם כפכפים), וגרביים ארוכות שקופות-למחצה
(`cfg.socks` עם `color`/`opacity`/`from` - garment על עקום הרגל, ממשיך
מ-`from` ועד למעלה, עם `transparent:true`). הצבעים שהאפיון לא פירט (שיער,
עור, עיניים) נבחרו חופשי - חום בהיר ועיניים כחולות-אפרפרות. חולצתה
כאפורה קיבלה גוון **כהה** (0x4a4f57) מראש, לא בינוני - לפי לקח טכני #3
(גוון בינוני נשטף כמעט ללבן תחת התאורה).

**⚠️ לקח טכני #2 (מכנסי ספורט קצרים):** הרגל הבסיסית (מתחת לכל בגד)
נבנית תמיד בצבע המכנס (`legMat = pantsMat || skinMat`), כי במכנסיים ארוכים
(ברירת המחדל) הם מכסים את כל הרגל ואין טעם לבנות עור מתחתיהם שלעולם לא
ייראה. אבל ב"מכנסיים קצרים" ה"מכנס" עצמו הוא רק overlay חלקי על תחילת
הרגל (`cfg.pantsLength` קובע עד לאן) - **אם לא מתקנים גם את `legMat`,
הרגל "לובשת מכנסיים" לאורך כולו** למרות שה-overlay נגמר מוקדם, כי אין
עור מתחתיו. תוקן עם `legMat = (pantsMat && !cfg.shorts) ? pantsMat : skinMat`.
נמצא ברינדור, לא בקריאת קוד - שוב מוכיח את הכלל "לרנדר, לא לנחש".

**⚠️ לקח טכני #3 (אביזר קטן על עור - קשיח על עצם, לא chain):** אביזרים
קטנים (תגי קבוצה, קעקועים) חייבים `BIND = { bone: '...' }` ולא
`{ chain: '...' }`. `chainBinder` נועד למשטחי עור *רציפים* לאורך כל האיבר
(כמו הזרוע עצמה) - עיגול קטן ודחוס באמצע האיבר מבלבל אותו ומייצר משקלי
skinning מנוונים, שאחרי ה-skinning גורמים לחלק פשוט להיעלם. תגי הקבוצה
(chest/hipR) נבנו נכון מההתחלה; קעקועי פלייטי נבנו בטעות עם chain ותוקנו.
**לקח נוסף מאותו סבב דיבוג:** גם אחרי המעבר ל-bone, קעקוע אחד עדיין לא
הופיע - כי `MeshBasicMaterial` בברירת מחדל הוא `side: THREE.FrontSide`,
וכיוון הנורמל של עיגול שמסתובב לפי מסגרת N/B מקומית של עקום (לא לפי
"קדימה" הגלובלי הקבוע) לא תמיד פונה למצלמה. תוקן עם `side: THREE.DoubleSide`
על חומר הקעקוע. **ולקח שלישי:** צבע ז'קט בגוון בינוני (כמו 0x8a5a2e,
שנוסה לפלייטי) "נשטף" כמעט ללבן תחת התאורה החזקה של הזירה - צריך גוון
כהה משמעותית (כמו 0x1a1a1a של ברייק) כדי שיישאר קריא; פלייטי בסוף קיבלה
0x452c17. כל שלושת הבאגים האלה **לא נראו כשגיאה בקונסול** - רק ברינדור.

**⚠️ לקח טכני חשוב:** טקסטורות-קנבס שנועדו לשמש עם `material.color` לבן
ניטרלי (כשהצבע האמיתי חי בתוך הטקסטורה עצמה, לא בהכפלה) **חייבות**
`texture.colorSpace = THREE.SRGBColorSpace`, אחרת every צבע יוצא הרבה
יותר בהיר ממה שצויר בקנבס (שחור 0x1a1a1a יצא אפור בהיר אצל ברייק). שאר
טקסטורות הקנבס בקובץ (`ribbedKnit`/`denimTex`/...) לא תוקנו בכוונה, כי הן
תמיד מוכפלות בגוון קיים שכבר כויל לפי המראה הנוכחי - תיקון שם היה משנה
את המראה של סווארק/בו שכבר אושר.

**חמישה אויבים לפי רמות קיימים** (`ENEMY_LEVELS`): בו (75 חיים), זומי (95),
ברדי (120), זיק (150), בונדרילו (190). מנצחים → עולים רמה והאויב הבא מחליף
אותו; מפסידים → נשארים באותה רמה ואפשר לנסות שוב. `enemy` הוא `let` בדיוק
כמו `player`, ו-`switchEnemy` מפרקת את המודל ובונה את הבא - אותו דפוס בדיוק
כמו `switchPlayerCharacter`. ההחלפה עצמה קורית ב-`resetGame` (דרך
`pendingEnemyLevel`), כדי שהאויב שהובס יישאר על המסך מאחורי מסך הניצחון עד
שמתחילים את הקרב הבא. הכותרת למעלה מציגה "דמות מול אויב - רמה X/5".
יכולות חדשות בבונה שנוספו בשבילם: אף-מקדחה (`cfg.drillNose`), חוטם תנין
עם שיניים (`cfg.snout`), עגיל באוזן/בלשון (`cfg.earRing`/`cfg.tongueRing`),
קוביות בטן וצלקת כדקאל שקוף (`cfg.abs`/`cfg.bellyScar`), סמל על התחתונים
(`cfg.heroShield`), חצאית/קימונו על הרגליים (`cfg.skirt`, עם `torn` לקצה
משונן), שוליים קרועים לחולצה (`cfg.tornHem`+`cfg.topHem`), וטקסטורה משלה
לחולצה (`cfg.topTex`, למשל `absShirtTex` - חולצה בצבע גוף עם קוביות בטן
מצוירות עליה, שזיק לובש).

**⚠️ לקח טכני #6 (ציור על מקום מסוים בבגד):** רוב מרקמי הבד חוזרים סביב
הגוף (`repeat` גדול מ-1), אבל ציור שאמור להופיע *פעם אחת על החזית* חייב
`repeat(1,1)` ומיקום מפורש בקנבס. ב-`sweepShapedEdge` הציר האופקי של ה-UV
הוא הזווית סביב הגוף - **החזית היא u=0.75** (θ=3π/2, אותה מוסכמה כמו בכל
שאר הקוד) - והציר האנכי הולך מהשוליים התחתונים (v=0) לכתפיים (v=1).
three הופכת טקסטורות אנכית, ולכן ראש הקנבס = הכתפיים. הקבוע `FRONT_U`
מחזיק את 0.75 במקום אחד.

**⚠️ לקח טכני #4 (יחידות R):** כל המספרים בבניית הראש/הפנים הם **ביחידות
R** (רדיוס הגולגולת) ומוכפלים ב-R בנקודת השימוש. הגדרת עזר כמו
`const tip = R * 2.30` ואז שימוש ב-`R * (0.62 + tip)` מכפילה ב-R פעמיים -
המקדחה של זומי יצאה באורך אפס ונבלעה בתוך הפנים. **לשמור קבועי-עזר
ביחידות R נקיות** ולהכפיל רק בשימוש. גם כאן: אפס שגיאות קונסול, נמצא רק
ברינדור מזווית צד (מלפנים חרוט שמצביע למצלמה נראה כמו נקודה).

**⚠️ לקח טכני #5 (דקאל על בגד):** `bodyDecal` מרחף מעל פני הגוף במרחק
`lift`. על עור חשוף 0.005 מספיק, אבל דקאל שיושב *על בגד* חייב `lift` גדול
מעובי הבגד (ה-`pad` שלו, בדרך כלל 0.011) - אחרת הוא נבלע בתוך הבגד
ופשוט לא נראה. הסמל על התחתונים של זומי הצריך 0.020.

**הלוט בקרב קיים** (`renderLootBar`/`useLootItem`): שורת פריטים בתחתית
המסך בזמן קרב, שמציגה **כל** אוכל/משקה/מגן שנקנה בחנות. לוחצים על פריט →
משתמשים בו: 🍎 אוכל מרפא חיים לפי המספר באפיון, 🥤 משקה נותן +3 כוח
ו-+4 מהירות (משקה אנרגיה כפול, כי האפיון מתאר אותו כ"מיוחד וחזק"),
🛡️ מגן חוסם נזק לגמרי למשך כמה שניות (`SHIELD_SECONDS`, נגזר מהתיאור
המילולי באפיון). **לארבעת המגנים היקרים יש יכולת מיוחדת** (`SHIELD_ABILITY`)
שמופעלת **אוטומטית** ברגע שמכה נחסמת בהם - בדיוק כמו שהאפיון דורש
("כאשר נשק פוגע בו"): ⚡ ברק (-15 לתוקף), 🧲 דבק (מושך את התוקף 0.55
לכיוונך וגורם לו את נזק המכה), 🔁 משכפל (מחזיר את הנזק **כפול**),
ו-🔁 בלתי נראה (מחזיר כפול, ובנוסף הדמות נעשית שקופה למחצה כל עוד המגן
פעיל - `applyInvisibility`). היכולת מוצגת כהודעה גדולה (`#shieldToast`)
עם הבזק צהוב, והאייקון שלה מופיע על כפתור המגן בלוט. **שים לב להיפוך
ב-`tickCombat`:** היכולת פוגעת ב*תוקף*, ולכן פס החיים שמתעדכן הוא של
התוקף (`who`) ואם הוא נופל מזה - המנצח הוא בעל המגן (`otherWho`). **כל פריט פעם אחת בקרב** - הוא יורד מהלוט של הקרב הנוכחי
(`lootUsed`) אבל *נשאר במלאי הקבוע* וחוזר בקרב הבא, בדיוק כמו שכתוב
באפיון בסעיפים 7-8. שורה שנייה (`#buffRow`) מציגה את ההשפעות הפעילות.
הכוח מתווסף ב-`tickCombat` והמהירות ב-`moveFighter`. **הבדל מהאפיון:**
האפיון מדבר על בחירה מקדימה של "בדיוק אוכל אחד, משקה אחד ומגן אחד"
(סעיף 5) - כאן מוצג הכל בלי מסך בחירה, כי זה מה שהמשתמש ביקש.

**⚠️ לקח טכני #7 (צילום אנימציות CSS):** צילום מסך חסר-ראש **לא תופס**
פריים באמצע אנימציית CSS מורכבת (opacity+transform) - ההודעה של המגן
יצאה ריקה בצילום למרות שהיא באמת מוצגת. הדרך לאמת אנימציה היא לדגום
`getComputedStyle(el).opacity` בלולאה (ראו שהיא עולה 0→1), ולצילום
"איך זה נראה" להקפיא את האלמנט זמנית ב-`style.cssText`. **לא להסיק
מצילום ריק שהאנימציה לא עובדת.**

**שדרוגים קיימים** (`characterLevels`/`upgradeCharacter`): קטגוריית "שדרוגים"
בחנות מציגה כרטיס לכל דמות **שכבר קנית** (לא כל 11 הדמויות - רק את שלך),
עם שתי רמות נפרדות: 💪 כוח ו-⚡ מהירות, כל אחת 1 עד `MAX_UPGRADE_LEVEL`
(11), כל אחת נקנית ומשודרגת בנפרד ובמחיר עולה (`upgradeCost(level) = level
* 10` - 10₪ לרמה 1→2, עד 100₪ לרמה 10→11). הרמות נשמרות **בנפרד לכל דמות**
(מפתח `characterLevels[name]`), כך שליזי ברמת כוח 5 נשארת שם גם אם עוברים
לג'ני ומשדרגים אותה בנפרד. **חיבור לקרב:** כוח/מהירות **הבסיסיים** של כל
דמות (`cfg.power`/`cfg.speed`, 0-10) כבר היו קיימים בטבלה ב-`game-spec.md`
סעיף 3 בלי שום השפעה בפועל - עכשיו יש. `refreshPlayerCombatStats` מחשבת
`player.powerMult`/`player.speedMult` (יחסית לסווארק, 5/10 = קו הבסיס =
מכפיל 1) כפול בונוס 5% לכל רמת שדרוג מעל 1. `tickCombat` מכפיל את נזק
האגרוף/הבעיטה (לא נזק נשק - **"חוץ מהנשקים"**, בדיוק כמו שהמשתמש ביקש)
ב-`powerMult`; `moveFighter` מכפיל את מהירות ההליכה ב-`speedMult` (מוכפל
גם בבונוס המשקה הקיים, לא מחליף אותו). המכפילים מחושבים מחדש ונשמרים על
`player` (לא מחושבים בכל פריים) - ב-`switchPlayerCharacter` וגם בכל שדרוג
שנקנה, בדיוק כמו ש-`weaponDmg` כבר נשמר על הלוחם בעת ציוד נשק. אויבים
תמיד נשארים על מכפיל 1 (אין מערכת שדרוגים לאויבים).

**קפיצה קיימת**: מקש רווח (Space) - היה כבר "צפוי" מבחינת מניעת גלילת
הדפדפן (`WATCHED`) בלי שום שימוש בפועל, עכשיו יש. `jumpRequested` נדלק
פעם אחת בלחיצה (edge-triggered ב-`keydown`, לא כל עוד המקש לחוץ) כדי
שהחזקת המקש לא תגרום לקפיצות רצופות. `startJump`/`tickJump` הן פיזיקה
פשוטה של מהירות כלפי מעלה שדועכת (`JUMP_VELOCITY`/`GRAVITY`) - `jumpY`
מתווסף לגובה עצם השורש **באותה שורה שה"נשימה" (`f.bob`) כבר משתמשת בה**
(`f.byName.root.position.y = J.root.y + f.bob + f.jumpY`), כי זו כבר
הנקודה שמזיזה את כל הדמות אנכית. קפיצה כרגע היא תזוזה/כיף בלבד - לא
עוזרת להתחמק ממכות (הבדיקה של פגיעה היא רק לפי מרחק אופקי `x`, לא לפי
גובה), ואפשר להוסיף את זה בהמשך אם ירצו.

**⚠️ לקח טכני #8 (waitForTimeout לא מריץ את לולאת המשחק בזמן אמת):**
כדי לבדוק קרב אמיתי (מכה שצריכה זמן אמיתי כדי להגיע לרגע הפגיעה שלה),
`page.waitForTimeout(500)` **לא מספיק אמין** - בבדיקה אחת המכה "לא פגעה"
בכלל למרות שכל הלוגיקה הייתה נכונה. הסיבה: `waitForTimeout` רק ישן בצד
Node, ולא מבטיח שהדפדפן ממשיך להריץ `requestAnimationFrame` בקצב אמיתי
בסביבה חסרת-ראש. הפתרון שעבד: **לדחוף פריימים ידנית** בלולאה -
`for (let i=0; i<N; i++) await page.evaluate(() => new Promise(r =>
requestAnimationFrame(r)));` - כל קריאת `evaluate` כזו מכריחה את הדפדפן
לצייר פריים אמיתי. זה מרחיב את לקח #7 (על צילומי מסך שמפספסים אנימציית
CSS) לבעיה עמוקה יותר: **כל בדיקה שתלויה בהתקדמות זמן-אמת של לולאת
המשחק (`animate`/rAF) חייבת לדחוף פריימים ידנית, לא לישון ולקוות.**

**⚠️ הערה חשובה על הסעיף הזה:** עד 2026-09-18 החלק הזה של `CLAUDE.md`
היה **מיושן משמעותית** ביחס לקוד בפועל - כמה שיחות מקבילות בנו פיצ'רים
ענקיים (איוונט שבועי, הרפתקה, ריאליזם, סומו, קרב קבוצות) בלי לעדכן כאן.
הסעיפים הבאים מתעדים את מה שנמצא בפועל בקריאת קוד + `פיתוח משחק.md`
באותו תאריך. **תמיד תעדיף את מה שכתוב ב-`פיתוח משחק.md` (היומן) על פני
הזיכרון - הוא מתעדכן בכל שיחה, זה (`CLAUDE.md`) לפעמים מפגר אחריו.**

**עדכון 2026-09-19:** בשלב הזה כל 11 הדמויות מהאפיון (`game-spec.md`
סעיף 3) כבר בנויות בקוד, וסולם האויבים (`ENEMY_LEVELS`) כבר גדל
מ-5 ל-10 רמות (עד לוסטר). **המשתמש ביקש עוד דמויות ועוד אויבים, והפעם
נתן החלטה מלאה על המראה** ("אתה תבחר איך הם יראו" - בניגוד לכל הדמויות/
האויבים הקודמים, שתוארו על ידו או לפי `game-spec.md`). נוספו שתי דמויות
קנייה חדשות (`TIGRO_CFG`/`ASTRO_CFG`, יקרות ומהירות/חזקות יותר מכל 11
המקוריות - טיגרו 850₪ לוחם-נמר עם פרווה כתומה מנומרת, אסטרו 1000₪
טייס-חלל עם חליפה כחולה זוהרת) ושני אויבים חדשים בסוף הסולם
(`MAGRON_CFG`/`ZARKON_CFG` - מגרון רמה 11 יצור געשי שחור עם סדקי לבה
זוהרים, זרקון רמה 12 רוח-סערה עם עיניים בצורת ברק). כל הארבע נבנו רק
משילובים חדשים של יכולות בונה-דמויות **קיימות** (לא נוספה אף יכולת
חדשה) - הסיכון היחיד היה בחירת גוונים/שילוב לא מוצלח, ואכן ניסיון
ראשון לאסטרו (חליפה לבנה עם "ג'טפאק" מ-`cfg.turtleShell`) נכשל ברינדור
ותוקן (ראה ההערה בקוד ליד `ASTRO_CFG`) - שוב "לרנדר, לא לנחש". סה"כ
עכשיו **15 דמויות קנייה** (כולל סינזר/פראנק של איוונט) ו-**12 רמות
אויב**. נבדק ברינדור מלא (Playwright + three.js
מקומי, ראה "לקחים שנקנו בדם" למטה): כל ארבעת הדמויות/אויבים נראים
כמו שתוכנן, קנייה אמיתית של טיגרו ואסטרו בחנות עובדת (יורד כסף, נכנס
למלאי), ומעבר לרמות 11-12 טוען את מגרון/זרקון עם ה-HP הנכון - אפס
שגיאות קונסול לאורך כל הבדיקות.

**עדכון 2026-09-19 (המשך):** נוספה דמות רביעית, **מאלפוי** (`MALFOY_CFG`,
1150₪, הכי חזק והכי מהיר בחנות הרגילה - הועלה מ-140₪/6.8/8 לפי בקשה
נוספת של המשתמש) - עם תיאור ויזואלי מפורט מהמשתמש (שיער בלונדיני, שזוף,
חולצה עם פסים אדומים וכתומים, ז'קט שחור עם סמל בית ספר גנרי, מכנסיים
אפורים). **לקח חדש:** חולצה מתחת לז'קט יכולה להיות **מוסתרת לגמרי** אם
`jacketHem` נשאר בברירת המחדל - הועלה בהדרגה (0.26→0.40→0.58) עד שבאמת
נראתה רצועת פסים ברורה מתחת לז'קט ברינדור. גם התגלה (ותועד גם ב"גיט"
למטה) שגם Cloud Run וגם GitHub Pages כבר עודכנו נכון בפעם הקודמת, אבל
**דפדפן של משתמש קצה יכול לתקוע גרסה ישנה בזיכרון** למרות רענון רגיל -
חלון גלישה-בסתר פתר וזיהה בוודאות שהבעיה בצד הלקוח, ותוקן שורש הבעיה
עם `Cache-Control: no-cache` ב-`nginx.conf` (רק על קלאוד ראן - לא ידוע
עדיין אם GitHub Pages סובל מאותה בעיה).

**עדכון 2026-09-19 (המשך שוב):** נוסף אויב שלוש-עשרה, **דראקו**
(`DRACO_CFG`, 650 חיים) - עיצוב שנבחר לגמרי על ידי (בקשת המשתמש שוב).
דרקון-לוחם כהה (עור ירוק-שחור קשקשי, חוטם+ניבים, קרניים, כנפיים, זנב,
טפרים) - נבחר במיוחד להיות **שונה** מלונג (רמה 7, גם "דרקון" אבל צהוב
עם כובע וחתול-שפם), כדי שלא יראה כמו העתק צבעוני. סה"כ עכשיו **16
דמויות קנייה** ו-**13 רמות אויב**.

**מסך הגדרות ושפה (עברית/אנגלית) קיימים** - המשתמש ביקש "עכשיו תעשה
את ההגדרות אבל תעשה רק את השפה עם תרגום אחד לאנגלית" (בעקבות תכנון
קודם ב-`פיתוח משחק.md` ל-2026-09-18 עם 4 כפתורים - שפה/גיפט/משתמש
חדש/חברים - שנעצר בלי קוד). נבנה מסך `#settingsOverlay` עם כפתור
גלגל-שיניים (⚙️, `#settingsBtn`) בלובי, בצד השני של כפתור ההתחברות עם
גוגל. **רק כפתור השפה פעיל** - שלושת האחרים (גיפט/משתמש חדש/חברים)
מוצגים כ-`disabled` עם "🚧 בקרוב", בדיוק כמו שתוכנן. `uiLang` ('he'/'en')
הוא מצב גלובלי, נשמר ב-`localStorage` (העדפת מכשיר, לא בענן - לא חלק
מ-`saveCloudState`). **מה מתורגם בפועל:** הלובי (כפתורי "קרב רגיל/סומו/
קבוצות" (`BATTLE_MODES`), חנות, הרפתקה, ציוד לקרב), **כל מסך החנות**
(כותרת, 6 תוויות הקטגוריות, תוויות הסטטיסטיקה "כוח/מהירות/נזק/מרפא/חינם"
דרך פונקציית `trStat`, תיאורי היכולות החופשיים של המגנים/משקה האנרגיה
דרך טבלת `SHIELD_ABILITY_EN`, כפתורי "קנה ב-"/"✅ שלך", וקטגוריית
השדרוגים), ומסך "ציוד לקרב" (כותרת+הסבר+כפתור חזרה). **⚠️ מה *לא*
מתורגם, בכוונה, לפי היקף שהמשתמש ביקש** ("רק תרגום אחד", לא "הכל כולל
שמות" שכבר הוחלט בעבר שזו עבודה ענקית מדי): שמות הדמויות/נשקים/אוכל/
משקאות/אויבים עצמם (נשארים תמיד בעברית - הם "שמות פרטיים", לא טקסט
ממשק), ומסכי הרפתקה/סומו/קרב-קבוצות/בנייה-עצמית/בוסים (עדיין רק
בעברית - אפשר להרחיב בעתיד אם ירצו). מנגנון הטכני: תוויות סטטיות
מתויגות ב-HTML עם `data-i18n-he`/`data-i18n-en`, ו-`applyLanguage()`
עוברת על כולן ומעדכנת גם את הרינדור הדינמי של החנות/כפתור מצב-הקרב אם
הם פתוחים כרגע - כדי שהתרגום ייכנס לתוקף מיד בלי לצאת ולהיכנס מחדש
למסך. נבדק ברינדור (Playwright): כל התרגומים מוצגים נכון בשני
הכיוונים (עברית↔אנגלית), כולל שמירה נכונה ב-localStorage. אפס שגיאות
קונסול.

**עדכון 2026-09-19 (עוד המשך) - תרגום מלא, כולל שמות:** המשתמש ביקש
"תתרגם גם את השמות של הדמויות לאנגלית ואת כל שאר הדברים" - הרחבה
משמעותית מעבר להיקף הקודם. נוסף `NAME_EN` - מילון גדול (81 שמות: 16
דמויות, 13 אויבים, 3 בוסים, 4 מפלצות-הרפתקה, 10 נשקים, 10 מגנים, 13
מאכלים, 12 משקאות) + `trName(name)` שמחזירה את השם האנגלי כשהשפה
אנגלית. **קריטי:** השם העברי נשאר תמיד המפתח הפנימי בכל מקום (inventory,
CHARACTER_CFGS, ENEMY_LEVELS, characterLevels וכו') - זה תרגום-תצוגה
בלבד, לא שינוי של איך המשחק שומר נתונים, כדי לא לשבור שום דבר קיים.
גם נוסף `T(he, en)` - קיצור כללי לבחירת מחרוזת לפי `uiLang`, לטקסט
דינמי שנוצר ב-JS (לא בא מ-HTML, אז `data-i18n` לא רלוונטי).

**מה עוד תורגם בסבב הזה** (מעבר למה שכבר תואר למעלה): כותרת הקרב
("X vs Y - Level N/13"), מסך ניצחון/הפסד המלא (כותרת, ציון, שורות
הסטטיסטיקה, כפתורים - בכל הענפים: קרב רגיל/הרפתקה/בוס), הלוט בקרב
ומסך "ציוד לקרב" (שמות הפריטים + תיאורי האפקטים), הודעות יכולת המגן
המיוחדת (⚡🧲🔁), שורת הבאפים הפעילים, מקרא המקלדת (A אגרוף/S בעיטה/וכו'),
תג הבוס בלובי, כפתור ההתחברות עם גוגל, מסכי הרפתקה/סומו/קרב-קבוצות
(כותרות, כפתורי עולמות, הוראות שליטה שמשתנות לפי עולם), מסכי "בנייה
עצמית"/"בניית נשק" של האיוונט (כולל כל הקטגוריות והאפשרויות), ותוצאות
סומו/קרב-קבוצות. **מגבלה ידועה, קוסמטית בלבד:** הדף נשאר `dir="rtl"`
גם באנגלית (לא הוחלף ל-ltr, כדי לא לסכן שבירת פריסה ב-CSS שנבנה
במפורש לימין-לשמאל) - כתוצאה מזה, פיסוק בסוף משפט (נקודה/סימן קריאה)
לפעמים "קופץ" להתחלת השורה החזותית בגלל כיווניות bidi. לא משפיע על
קריאות, רק קוסמטי. **נבדק ברינדור מקיף** (Playwright): כל 6 קטגוריות
החנות, מסך ציוד-לקרב, הרפתקה, ומסך ניצחון-מדומה - כולם הראו תרגום
מלא ונכון (למשל: "Svarok vs Bo - Level 1/13", "Victory! Next: Zoomi",
"Grade: S", "You: started with 100 HP..."). אפס שגיאות קונסול.

**איוונט שבועי קיים** (`isEventActive`, כל יום שישי 22:00-22:45 לפי
**שעון המחשב שמשחקים בו** - לא שרת): באנר מהבהב בלובי/בחנות, 30% הנחה
על כל פריט בחנות (`EVENT_DISCOUNT`, לא כולל שדרוגים). **בזמן איוונט
בלבד** נפתחים 4 מסכים/יכולות נוספות: (1) **"🎨 בנייה עצמית"** - בונים
דמות מ-8 קטגוריות מוכנות (שיער/עור/חולצה/מכנס/נעליים/גרביים/משקפיים/
זקן) + סליידרי כוח/מהירות 0-8.5, נשמרת ל-`CHARACTER_CFGS`+`inventory`
ומסומנת ב-`CUSTOM_CHARACTER_NAMES` (מסונן מחוץ לאיוונט גם בלובי וגם
בחנות); (2) **"🔨 בניית נשק"** - ראש (4 סוגים)+צבע+אורך ידית + סליידר
נזק 0-67, אותו דפוס (`CUSTOM_WEAPON_NAMES`); (3) **פראנק וסינזר** - שתי
דמויות מוכנות זמינות-איוונט-בלבד בחנות (200₪); (4) **"👹 קרב בוס"** -
שלושה בוסים (קראגור/פרוקנט/צ'ארס, נבחרים בשורת חצים) עם הרבה חיים
(260-320) ותגמול ניצחון **קבוע וגדול** (150-200₪, לא לפי נזק), בלי
עליית רמה, אפשר להילחם שוב ושוב (`BOSS_LEVELS`/`startBossFight`,
נפרד לגמרי מ-`ENEMY_LEVELS`). **מגבלה ידועה:** דמות/בוס-איוונט שכבר
נבחרו לפני שהאיוונט נגמר לא מוחלפים אוטומטית.

**הרפתקה קיימת** (כפתור "🗺️ הרפתקה" בלובי) - **ארבעה עולמות ענקיים**
(פי 7 מהגודל המקורי, ערפל רחוק כדי "לראות את העולם באופק"): מערת
הקריסטל, האי האבוד, מכתש הלבה (עם אגם לבה מרכזי שאי אפשר להיכנס אליו),
ויער הזומבים. כל עולם: קבוצת-סצנה נפרדת ומרוחקת מאוד מהזירה
(`CAVE_OFFSET`/`ISLAND_OFFSET`/`LAVA_OFFSET`/`FOREST_OFFSET`, על צירים/
כיוונים שונים כדי שלא "יראו" אחד את השני), תנועה חופשית דו-ממדית
בחיצים (נפרדת לגמרי ממערכת התנועה החד-ממדית של הקרב), אוצר מוסתר
**באמת** (טבעת עצמים גדולים עם פתח יחיד אחד שחוסמת קו-ראייה פיזית -
לא רק עמעום/מרחק, כי בעולם פתוח שום דבר אחר לא מונע לראות אובייקט
מרוחק), ו-2-3 מפלצות (בנויות משילובי-יכולות קיימים של בונה הדמויות,
בגוונים שמתאימים לעולם) שרודפות לפי טווח ראייה (`updateMonsterChase`,
משותפת לכל העולמות) ופותחות **קרב אגרוף אמיתי בתוך העולם עצמו** (לא
בזירה) כשמגיעים למגע - באמצעות `battleAnchorX/Z` (עוגן שמזיז את "איפה
שהקרב קורה" למיקום המפלצת, בלי לשכפל שום לוגיקת קרב). ניצחון על מפלצת
מסיר אותה לצמיתות (עד כניסה טרייה חדשה) בלי כסף (רק האוצר נותן כסף:
300/350/400/450₪ בהתאמה); הפסד משאיר אותה בחיים. יציאה מחזירה לזירה
הרגילה עם המיקום/מצב המקוריים.

**ריאליזם קיים - 4 שלבים הושלמו:** (1) **מרקמים** - `cfg.skinTex`/
`cfg.hairTex` (טקסטורת קנבס עדינה שמוכפלת בצבע האמיתי, לא מחליפה אותו)
על עור/שיער של **כל** הדמויות עם צבע עור אחיד (לא על עורות-מיוחדים כמו
`crystalSkin`), ו-`woodGrainTex`/`metalScratchTex`/`leatherTex` על כל
חלקי הנשקים (עץ/מתכת/עור לפי סוג האחיזה) - **כל 10 נשקי החנות ונשקי
"בנייה עצמית"**. (2) **תאורה/צללים** - הוקטנו האורות ה"שטוחים" (ambient/
hemisphere) והוגברו אור-מפתח/צל אמיתי, כדי שהגוף לא ייצא שטוח; הזירה
עצמה קיבלה אווירה כהה-דרמטית ("כמו שידור אגרוף אמיתי" - קהל דועך
לחושך, `Fog` שמכסה אותו, צבעי קהל מוכהים). (3) **קול** - `playImpact()`
מסנתז צלילי מכה (Web Audio API, לא קבצי שמע - עקבי עם "קובץ אחד") בארבעה
פרופילים (אגרוף/בעיטה/נשק/חסימה), נפתח רק אחרי הקליק הראשון (מסך
`#clickStartOverlay` - גם פותח פוקוס-מקלדת בתוך iframe, גם `AudioContext`).
(4) **פיזיקה** - כל מכה שפוגעת נותנת הדף (`knockVel`, דועך בחיכוך) +
הלם קצר (`staggerT`, לא יכולים לפעול); מכות חזקות (בעיטה/נשק, לא אגרוף)
גם **מפילות** ל-3 פאזות (נפילה→שכיבה→קימה, `down`/`fallProgress`).

**"ציוד לקרב" ושליטה במגע קיימים:** מסך בחירה מראש (כפתור בלובי) -
עד 2 מאכלים, משקה אחד, מגן אחד (`LOOT_CAPS`, בחירת המשתמש - שונה
מהאפיון) - ורק הנבחרים מוצגים בלוט בזמן קרב (`renderLootBar`, לא כל
המלאי כמו קודם). **שליטה במגע לטלפון** (בנוסף למקלדת, לא במקומה) -
ג'ויסטיק וירטואלי לתזוזה + 5 כפתורי פעולה, פועלים רק על `isTouchDevice`
(בדיקה חד-פעמית - במחשב הבלוק כולו לא רץ), כל כפתור פשוט "לוחץ" על
אותו דגל ב-`KEYS` שהמקלדת כבר משתמשת בו כדי שלוגיקת הקרב לא תזוז בכלל.
מסך "סובב את הטלפון" חוסם משחק במצב לאורך.

**סומו וקרב קבוצות קיימים** - שני "עולמות זירה" נוספים, נבחרים בשורת
חצים ליד "התחל קרב" (יחד עם "קרב רגיל"): **סומו** (`enterSumo`) - עיגול
עם תנועה חופשית דו-ממדית, בלי הכות בכלל - רק דחיפה ("משיכת חבל" לפי
`powerMult` של כל צד, כולל שדרוגי כוח מהחנות), מי שנדחף מעבר לרדיוס
נופל ומפסיד; תגמול קבוע (`15 + enemyLevel*5`₪), בלי חיים/ציון. **קרב
קבוצות** (`enterTeamBattle`, עד 6 לוחמים) - 2 נגד 2 או 2 נגד 4 (נבחר
במסך ביניים), אותו קרב אגרוף/בעיטה/אבן בדיוק כמו הזירה הרגילה
(`tickCombat`/`ATTACKS` משותפים לגמרי) עם שתי הרחבות: `nearestOpponent`
(יעד תקיפה דינמי במקום יריב יחיד קבוע) ו-`resolveTeamOverlaps` (מונע
חפיפה בין כל הזוגות, לא רק שניים). תנועה **חופשית דו-ממדית עם סיבוב**
(חצים שמאלה/ימינה מסובבים, למעלה/למטה מתקדם/נסוג לפי הכיוון) - נבדק
מפורשות שהאנימציות (שמכוילות לזווית עמידה קבועה) לא נשברות כי הן
מחושבות ביחסי-עצם מקומיים, לא תלויות בסיבוב הכללי. מי שמגיע לאפס חיים
נופל **לצמיתות** (`koPermanent`, לא קם יותר). תגמול `15 * מספר האויבים`₪.

**⚠️ שלושה לקחים כלליים חדשים מבניית ההרפתקה/סומו/קרב-קבוצות:**
1. **יחסי הורה-ילד במרחב לא רק סיבובים** - קבוצת-סצנה עם offset
   (`CAVE_OFFSET` וכו') הופכת את `position` של הילדים שלה ליחסי, לא
   מוחלט; מצלמה/לוגיקה שמשתמשות במיקום הזה ישירות בלי לחבר את ה-offset
   "רואות" את המקום הלא-נכון. נתפס ברינדור (המצלמה נשארה תקועה ליד
   הזירה במקום להיכנס למערה).
2. **לא כל באג נתפס ברינדור** - באג של "המסך לא מגיב" (זרימת מסכים/
   state machine, כמו חזרה-למערה-אחרי-הפסד-פותחת-קרב-מיד, או
   `sumoResultOverlay` שנשאר תקוע וחוסם קליקים) נתפס הרבה יותר מהר עם
   `console.log`/בדיקת DOM ישירה מאשר בניחוש מקוד או בצילום מסך.
3. **קיר עגול דורש clamp, לא דחיית-צעד מלאה** - אם תנאי-גבול בודק
   "התנועה תקפה במלואה או שלא זזים בכלל", הליכה *לאורך* קיר עגול
   (כמו שפת אגם הלבה) "נתקעת" בכל פעם שצעד טבעי נוגע קלות בגבול. התיקון:
   תמיד לכווץ/להגדיל את וקטור התזוזה לגבול התקף הקרוב ביותר ולעדכן,
   לא לדחות.

**גיפטים קיימים** (2026-09-19, בקשת המשתמש "עכשיו תעשה את הגיפטים",
**עם אבא בשיחה** - כנדרש בכלל הקבוע למעלה כי זו החלטת שרת/דאטהבייס):
אפשר לשלוח לחבר דמות/נשק/אוכל/משקה/מגן שברשותך (לא כסף אמיתי). מסך
חדש (`#giftOverlay`, נפתח מ-⚙️ הגדרות → 🎁 גיפט): כל שחקן מחובר בוחר
**שם משתמש** (`myUsername`, מוצג לאחרים - לא השם האמיתי מגוגל, הגנת
פרטיות שאבא ביקש), ואז רואה רשימת **כל** השחקנים האחרים עם שם משתמש
(שאילתת `getDocs(collection(db,'players'))` - כל שחקן יכול לקרוא את כל
מסמכי `players` עכשיו, לא רק את שלו). לוחצים על שחקן → פרופיל (שם,
כסף, כל הפריטים שלו לפי קטגוריה) → "שתף פריט שלי" → רואים **את המלאי
של עצמכם** (לא שלו) ובוחרים מה לשלוח. **"שיתוף" מוריד מהשולח** - לא
העתקה. **מגבלה:** אי אפשר לשתף את הדמות/נשק **היחיד** שנשאר לך (הכפתור
מושבת עם "(היחיד שלך)") - כדי שלא תישאר בלי כלום לשחק איתו; אוכל/משקה/
מגן אפשר לשתף הכל. מנגנון: `sendGift` קודם כותב מסמך ב-
`players/{מי-שמקבל}/incomingGifts/{id}` (עם `kind`/`itemName`/`fromUid`/
`fromUsername`), ורק **אחרי הצלחה** מוריד את הפריט מהמלאי שלך ושומר;
`checkIncomingGifts` רץ בכל התחברות, לוקח כל "מתנה ממתינה" שיש, מוסיף
למלאי שלך, ומוחק אותה - עם הודעת "קיבלת מתנה" (`showShieldToast`).
אם הפריט שנשלח/שהתקבל הוא הדמות/נשק המצוידים כרגע - מוחלף אוטומטית
לפריט אחר (`cycleCharacter`/`cycleWeapon`) לפני שהוא נמחק, כדי שלא
תישארו בלי דמות בכלל.

**⚠️ החלטת אבטחה שהתקבלה עם אבא (לא הוחלטה לבד, לפי הכלל הקבוע):**
בלי שרת אמיתי (Cloud Functions דורש תוכנית Blaze בתשלום) - **רק כללי
Firestore** קובעים מה מותר. המשמעות המדויקת: השיתוף הוא **שתי כתיבות
נפרדות** מהדפדפן של השולח (קודם כותבים "מתנה" אצל החבר, אחר כך מורידים
מעצמכם) - **לא פעולה אטומית אחת**. ילד שפותח כלי-מפתחים (devtools)
בדפדפן יכול תיאורטית לדלג בכוונה על שלב ה"הורדה מעצמו" ולשכפל פריט
לעצמו בלי לאבד אותו. **אבא אישר שזה מקובל** - זה סיכון-רמאות *בתוך
המשחק* בין חברים, לא סיכון לחשבון גוגל האמיתי של אף אחד (ה-OAuth של
גוגל לא נחשף לעולם - רק שם המשתמש שבחרתם ונתוני ההתקדמות במשחק נהיים
גלויים לשחקנים אחרים מחוברים). כלל האבטחה **כן אוכף** שאי אפשר לשתף
פריט שאין לכם (בודק עם `get()` שהמלאי שלכם כולל את הפריט **ברגע יצירת
המתנה**) - זה סוגר את הפרצה הקלה יותר של "מתנה משום מקום".

**⚠️ שאלה פתוחה שאבא העלה ולא הוכרעה סופית:** אבא הציע חלופה חזקה
יותר - קלאוד ראן (שכבר פרוס ובוודאי עם חיוב פעיל ב-Google Cloud, בלי
קשר לתוכנית Blaze של Firebase) יכול לשמש כשרת אמיתי (עם Firebase Admin
SDK) שמבצע את ההעברה בפעולה אחת אטומית ובלי שום פרצה. זה דורש קוד שרת
חדש ומפתח Admin חדש (סוד נוסף, נשמר בדיוק כמו `GCP_SA_KEY` הקיים).
**לא נבנה** - הגרסה שנבנתה היא הפשוטה (כללי Firestore בלבד). **עדיין
לא ידוע אם אבא רוצה לשדרג לזה בעתיד** - לשאול שוב לפני שסוגרים את
הנושא לגמרי.

**⚠️ שלב ידני נדרש (מבוגר) - הפיצ'ר לא באמת עובד בלי זה:** צריך להיכנס
ל-Firebase Console → Firestore → Rules ולהדביק כללים חדשים (כתובים
בהודעה שנשלחה לאבא/למשתמש בשיחה שבה זה נבנה) - זה **חובה** כדי שקריאת
כל השחקנים ומנגנון המתנות בכלל יעבדו; בלי זה, המשחק ימשיך לעבוד רגיל
אבל מסך הגיפט יציג שגיאת הרשאות. **קלוד לא יכול לעשות את זה בעצמו** -
זו כניסה לאתר חיצוני עם חשבון גוגל, בדיוק כמו שהכלל למעלה על "שלבים
באתרים חיצוניים" מסביר.

**נבדק ברינדור** (Playwright, `cloudUser`/`myUsername`/`giftMode` נחשפו
זמנית דרך `window.game` כדי לדמות שני שחקנים בלי גישה אמיתית ל-Firestore
מסביבת הפיתוח החסומה - אותה מגבלה שכבר קיימת לגבי בדיקת התחברות גוגל
אמיתית): מסך "לא מחובר" מציג הודעת התחברות נכונה, מסך "אין שם משתמש"
מציג שדה קלט, מסך פרופיל מציג שם+כסף+פריטים נכון לפי קטגוריה, מסך שיתוף
מציג רק את המלאי של עצמכם (לא של מי שצופים בו) ומשבית נכון את הדמות/נשק
היחיד שנשאר. אפס שגיאות קונסול מקוד המשחק (רק שגיאות רשת צפויות מניסיון
לגעת ב-Firestore האמיתי מתוך הסביבה החסומה - לא נבדק קצה-לקצה עם שני
משתמשים אמיתיים, כמו שגם התחברות גוגל אמיתית לא נבדקה כאן).

**חברים קיימים** (2026-09-19, "עכשיו תעשה את המסך חברים" - **נפרד
לגמרי מהגיפט**, לפי בחירת המשתמש בין 2 חלופות שהוצגו): מסך `#friendsOverlay`
חדש (⚙️ הגדרות → 👥 חברים, שהיה קודם "בקרוב") - **פרטי בהרבה** ממסך
הגיפט: **אין** רשימה גלויה של כל השחקנים. במקום זה, מקלידים שם משתמש
מדויק של מישהו ולוחצים "➕ שלח בקשת חברות" - הוא רואה בקשה ממתינה
בפעם הבאה שהוא נכנס למסך שלו, ויכול "✅ אשר" או "❌ דחה". רק אחרי אישור
שני הצדדים רואים אחד את השני ב"החברים שלך". **הגיפט לא השתנה בכלל**
(ממשיך להראות את כל השחקנים החתומים, בלי קשר לחברים) - זו הייתה בחירה
מפורשת של המשתמש ("שני מסכים נפרדים") כשהוצגו לו שתי האפשרויות (לגייט
את הגיפט להיות רק בין חברים, לעומת השארתו כמו שהוא). **שם המשתמש
משותף** בין שני המסכים (`myUsername`, אותו מנגנון בחירה/שינוי בדיוק -
`renderUsernameForm` הפכה לפונקציה כללית עם `targetEl`/`onDone` כדי
ששני המסכים ישתמשו באותו קוד בלי כפילות).

**טכנית (עם אבא, לפי הכלל הקבוע - עוד שינוי כללי Firestore):** שני
תת-אוספים חדשים תחת `players/{uid}`, **אותו דפוס בדיוק** כמו
`incomingGifts` שכבר קיים: (1) `friendRequests/{fromUid}` - בקשה
ממתינה (`fromUsername`, `createdAt`) - **מזהה המסמך הוא ה-uid של
השולח עצמו**, לא מזהה אקראי, כדי שכלל האבטחה יוכל לוודא זהות רק לפי
הנתיב בלי לבדוק `resource.data` בכלל (`allow create: if
request.auth.uid == fromUid`); קריאה/מחיקה רק לבעל התיבה. (2)
`friends/{friendUid}` - חברות מאושרת, **כל צד שומר עותק נפרד משלו**
(לא אוסף אחד משותף) - כשמאשרים, `acceptFriendRequest` כותבת גם
לעצמנו (רגיל - הבעלים כותב לעצמו) וגם ל"חבר" (`players/{fromUid}/friends/{שלי}`)
- זה **מותר** כי הכלל מרשה למישהו להוסיף את עצמו (`friendUid == auth.uid`)
לרשימת החברים של כל אחד, בדיוק כמו ש-`incomingGifts` מרשה למישהו
להוסיף לעצמו הודעה בתיבה של מישהו אחר. **לא צריך `get()`** כאן (בניגוד
לגיפטים) כי אין "בעלות על פריט" לבדוק - רק זהות, שהנתיב כבר מוודא.
חיפוש שחקן לפי שם משתמש (`sendFriendRequest`) משתמש ב-
`query(collection(db,'players'), where('username','==',...))` - מותר
תחת אותו כלל read הקיים (לא צריך כלל נוסף לשאילתות מסוננות).

**⚠️ שלב ידני נדרש (מבוגר), שוב:** כללי ה-Firestore ב-Firebase Console
צריכים להתעדכן **עוד פעם** עם שני הבלוקים החדשים (`friendRequests`/
`friends`) בנוסף למה שכבר שם מהגיפטים - בלי זה, שליחת/אישור בקשות
חברות ייכשל בשקט (שגיאת הרשאות בקונסול, לא קריסה). קלוד לא יכול
להדביק את זה בעצמו.

**נבדק ברינדור** (Playwright, אותה שיטת דימוי כמו הגיפט - `cloudUser`/
`myUsername` דרך `window.game`): מסך "לא מחובר" ומסך "אין שם משתמש"
מוצגים נכון, ומסך ראשי (עם שם משתמש) מציג "אתה: X", שדה הוספת חבר,
וסעיף "החברים שלך" ריק כברירת מחדל. אפס שגיאות קונסול. **לא נבדק
קצה-לקצה** (בקשה אמיתית + אישור אמיתי בין שני משתמשים) - אותה מגבלה
כמו הגיפט, ודורש גם את עדכון כללי ה-Firestore קודם.

**"קרב מול חבר" - שלב 1 קיים (2026-09-20, "תעשה שאפשר לשחק איתם
ביחד"):** מסך חדש `#matchOverlay` - הזמנה לקרב מתוך רשימת החברים
(כפתור "⚔️ הזמן לקרב" ליד כל חבר) + "חדר המתנה" משותף שמוכיח שהחיבור
בזמן אמת בין שני דפדפנים עובד. **זו עדיין לא קרב אמיתי** - שלב 1 מתוך
3 שתוכננו (2: רואים את החבר זז בזירה; 3: קרב מלא). `matches/{matchId}`
(מסמך משותף, `matchId` = שני ה-uid-ים ממוינים) עם `onSnapshot` -
ה-listener הראשון בפרויקט שמאזין לשינויים בזמן אמת (בשונה מ-`getDocs`
החד-פעמי שגיפטים/חברים השתמשו בו עד עכשיו). `players/{uid}/battleInvites/{fromUid}`
זהה לגמרי לדפוס `friendRequests`. **⚠️ שלב ידני נוסף לאבא** - עוד שני
בלוקים בכללי ה-Firestore (`battleInvites`+`matches`), פרטים ביומן
הפיתוח ל-2026-09-20. **נבדק ברינדור**: מסך ההמתנה מוצג נכון בעברית
ובאנגלית, כפתור ביטול לא קורס. **לא נבדק קצה-לקצה** (שני משתמשים
אמיתיים - דורש את עדכון הכללים קודם).

**לא קיים עדיין:** מסך מלאי נפרד, ציונים, ואנימציית מכה ייחודית לכל
נשק. קפיצה לא עוזרת להתחמק ממכות (הבדיקה היא לפי מרחק אופקי בלבד).
שלבים 2-3 של "קרב מול חבר" (תנועה משותפת בזירה, וקרב אמיתי מול חבר).

מבנה השלד: `SKEL`/`J`/`BONE_TREE` בקוד מגדירים את העצמות, `chainBinder`
מחשב לכל קודקוד באיברים המתעוותים (רגליים, זרועות, גוף) לאיזו עצם הוא שייך
ובאיזה משקל, ו-`toSkinned` הופך מש רגיל למש עם Skinning אמיתי. חלקים שלא
צריכים להתעוות (ראש, שיער, אגרופים, כפות רגליים) פשוט תלויים קשיח על עצם
אחת עם `attach`. כל תנוחה (הליכה, אגרוף, בעיטה, מכת אבן, חסימה) היא סטייה
מזוויות אפס, שהן בדיוק עמידת הקרב המקורית - `poseFighter` מחשב לאן כל עצם
צריכה להגיע ו"מחליק" אליה עם exponential smoothing כדי שלא יהיו קפיצות.

מבנה הקרב: `ATTACKS` מגדיר לכל מכה משך, רגע שיא (לאנימציה), סף פגיעה (`hitAt`
- לא טווח, כדי לא לפספס פגיעה בקצב פריימים איטי), נזק וטווח - מקום אחד לכל
המספרים. `tickCombat` בודק כל פריים אם למכה הפעילה יש "נגיעה": בתוך הטווח,
עברה את הסף, ועוד לא הופעלה (`hitDone`). חסימה בודקת רק את הדגל הבוליאני
`blocking`, לא את העוצמה החזותית `blockAmt`.

## דפלוי (איך המשחק מגיע לאנשים אחרים חוץ מדאבל-קליק)

**⚠️ עדכון 2026-09-19: GitHub Pages הופסק - האבא אישר במפורש שאף אחד
לא משתמש בו, רק בקלאוד ראן.** אין יותר צורך לדחוף לענף ה-Pages או
לוודא deployment שם - ראה סעיף "גיט" למטה, שם ההוראה עודכנה בהתאם.
הקישור הישן (`https://galdotan50.github.io/Battle-game`) עשוי עדיין
להציג גרסה ישנה שקפאה בזמן - **לא לשלוח אותו יותר למישהו כ"הקישור
למשחק"**.

**שתי דרכים קיימות (הפעילה בפועל היא רק #2 - קלאוד ראן):**

1. ~~**GitHub Pages**~~ (הופסק, ראה למעלה) - `https://galdotan50.github.io/Battle-game`.
2. **Google Cloud Run** - `https://gals-battle-game-82960354590.us-central1.run.app`
   **(הקישור היחיד שבאמת נמצא בשימוש - זה מה ששולחים למישהו שרוצה לשחק)**
   - פרויקט Google Cloud בשם `gals-battle-game` (של האבא, לא של המשתמש-
   הילד - הוקם ב-2026-09-18 בשיחה עם האבא). `Dockerfile`+`nginx.conf`
   בשורש הריפו עוטפים את `index.html` בשרת nginx סטטי (`listen 8080`,
   כפי ש-Cloud Run דורש). **דפלוי אוטומטי דרך GitHub Actions**
   (`.github/workflows/deploy-cloud-run.yml`) - רץ בכל push ל-`main`,
   **וגם ניתן להפעלה ידנית** מתוך שיחה עם קלוד (`mcp__github__actions_run_trigger`
   עם `method:"run_workflow"`, `workflow_id:"deploy-cloud-run.yml"`,
   `ref:"main"`) - כלומר אפשר "לבקש דפלוי" סתם בצ'אט בלי לגעת בטרמינל.
   ה-workflow מתחבר ל-Google Cloud עם **service account** (`github-deployer@
   gals-battle-game.iam.gserviceaccount.com`) שהמפתח שלו שמור כ-GitHub
   secret בשם `GCP_SA_KEY` (בהגדרות הריפו) - **לא** בקוד, לא ביומן, לא
   כאן. אם צריך להנפיק מפתח חדש (למשל הישן נחשף בטעות): `gcloud iam
   service-accounts keys create KEY.json --iam-account=github-deployer@
   gals-battle-game.iam.gserviceaccount.com`, ואז להדביק את תוכן הקובץ
   בעמוד ה-secret ב-GitHub (**אף פעם לא בצ'אט עם קלוד** - זה לא מקום
   מאובטח לאחסן סוד, גם אם קלוד "מקבל" אותו, כי זה נשמר בהיסטוריית
   השיחה). **חשוב:** קלאוד ראן ופיירבייס/פיירסטור (הסעיף הבא) **לא
   מדברים ביניהם בכלל** - הדפדפן של מי שמשחק הוא זה שמדבר עם שניהם
   בנפרד (מוריד את `index.html` מקלאוד ראן, שומר/טוען נתונים ישירות
   מול פיירסטור). שני השירותים חיים באותו פרויקט Google Cloud רק
   מטעמי נוחות ניהולית, לא כי הם תלויים זה בזה.
3. **Firebase (Auth + Firestore) - קיים ופעיל בקוד (הושלם ב-2026-09-18):**
   אותו פרויקט `gals-battle-game` (Firebase חובר אליו, לא נוצר פרויקט
   נפרד). Google Sign-In מופעל ב-Authentication, Firestore קיים (מסד
   `(default)`, מיקום `nam5`). **חוק האבטחה המקורי** (מלפני הגיפטים):
   כל שחקן קורא/כותב רק למסמך `players/{uid}` שלו. **⚠️ עדכון 2026-09-19
   בשביל הגיפטים** (ראה סעיף "גיפטים קיימים" למעלה) - חוק חדש נדרש (כתיבה
   מותרת רק לבעלים כמו קודם, אבל **קריאה** מותרת לכל שחקן מחובר + תת-
   אוסף `incomingGifts` תחת כל שחקן, שכל שחקן *אחר* יכול ליצור בו מסמך
   (בתנאי שהוא מוכיח עם `get()` שהפריט קיים אצלו במלאי) אבל רק הבעלים
   יכול לקרוא/למחוק. **זה שינוי שדורש הדבקה ידנית ב-Firebase Console
   ע"י מבוגר - קלוד לא יכול לעשות את זה בעצמו.** כתובת קלאוד ראן רשומה
   כ-domain מורשה ב-Authentication. **קוד ההגדרה (לא סודי, מוטמע
   ב-`index.html` בפועל):**
   ```js
   const firebaseConfig = {
     apiKey: "AIzaSyDQdlhNciuQatOhXY14gVfRA2osZX67CGk",
     authDomain: "gals-battle-game.firebaseapp.com",
     projectId: "gals-battle-game",
     storageBucket: "gals-battle-game.firebasestorage.app",
     messagingSenderId: "82960354590",
     appId: "1:82960354590:web:9cde9d49ad8803902ecd0f"
   };
   ```
   **מה שומר בפועל (`saveCloudState`/`applyCloudState`/`buildCloudState`
   ב-`index.html`):** כסף, `inventory`, `characterLevels`,
   `selectedCharacterName`/`selectedWeaponName`, `enemyLevel`, `wins`/
   `losses` (נספרים רק מול סולם האויבים הרגיל `ENEMY_LEVELS`,
   לא סומו/קרב קבוצות/בוסים/הרפתקה, שיש להם כבר תגמול נפרד משלהם), ו-
   `username` (חדש מ-2026-09-19, בשביל הגיפטים - שם שמוצג לשחקנים
   אחרים, לא השם האמיתי מגוגל).
   **לא כולל עדיין** דמויות/נשקים שנוצרו ב"בנייה עצמית"/"בניית נשק"
   באיוונט (הוחלט מפורשות לדחות לשלב נפרד בעתיד - ראה `פיתוח משחק.md`
   ל-2026-09-18). כפתור **"🔵 התחבר עם גוגל"** בפינה הימנית-עליונה של
   הלובי (`cloudSignInBtn`) - התחברות **אופציונלית לגמרי**, בלי להתחבר
   המשחק עובד בדיוק כמו קודם (הכל רק בזיכרון). מתחברים
   (`onAuthStateChanged`) → טוען מסמך שמור אם יש, או יוצר אחד חדש עם
   המצב הנוכחי אם זה שחקן ראשון. `saveCloudState()` נקרא אחרי כל שינוי
   משמעותי (קנייה ב-`buyItem`, שדרוג ב-`upgradeCharacter`, החלפת
   דמות/נשק, עליית רמה ב-`resetGame`, וסיום כל קרב ב-`endGame`). בלי
   שרת משלנו שמאמת כתיבות - Firestore Security Rules הן ההגנה היחידה
   (בהסכמת האבא: מספיק למשחק משפחתי, לא חסין-רמאות נגד מי שפותח
   devtools בכוונה). ה-SDK נטען ב-`index.html` דרך ה-CDN של Firebase
   (importmap עם `firebase/app`/`firebase/auth`/`firebase/firestore`
   מצביעים ל-`https://www.gstatic.com/firebasejs/10.14.1/...`, ES
   modules אמיתיים - לא npm/bundler), כדי לא לשבור את אילוץ "קובץ אחד,
   בלי build". **נבדק ברינדור** (three.js+Firebase הותקנו מקומית
   ואוגדו עם esbuild כקבצי ESM עצמאיים, כי unpkg/gstatic חסומים ברשת
   הפיתוח - ראה "לקחים שנקנו בדם" למטה): לובי עולה נקי עם כפתור
   ההתחברות, אפס שגיאות קונסול, קנייה אמיתית של שחקן-אורח עדיין מורידה
   כסף/מוסיפה למלאי, ומכה אמיתית עדיין גורמת נזק אמיתי - בדיוק כמו
   לפני השינוי. **לא נבדק כאן** (ולא ניתן להיבדק בסביבת הפיתוח): זרימת
   ההתחברות האמיתית מול גוגל (דורשת חשבון גוגל אמיתי ודפדפן אמיתי) -
   צריך בדיקה ידנית של המשתמש בדפדפן אמיתי.

## גיט

**⚠️ קריטי - חובה לקרוא לפני שנוגעים בקוד:** כל שיחה חדשה מקבלת **ענף
אקראי משלה** (למשל `claude/nifty-dirac-6inx9n`) מהמערכת שמריצה את קלוד -
זה לא משהו שנשלט מתוך הריפו, וזה קורה **בכל שיחה מחדש**, גם אחרי שהריפו
כבר קיבל ענף `main` אמיתי. **אי אפשר להניח שהענף שהוקצה לשיחה הזו הוא
העדכני ביותר** - זה כבר קרה כמה פעמים: שיחה בנתה פיצ'ר על בסיס ישן, בלי
לדעת ששיחה אחרת כבר הוסיפה משהו אחר במקביל, ושני הפיצ'רים "נעלמו" זה
מזה עד שמישהו שם לב ומיזג ידנית.

**`main` הוא "מקור האמת" לקוד, וממנו קלאוד ראן מתעדכן אוטומטית.**
**לפני שמתחילים לכתוב קוד בכל שיחה:**

1. `git fetch origin` ולהריץ `git ls-remote origin` - לראות את כל הענפים.
2. לבדוק אם הענף שהוקצה לשיחה הזו הוא צאצא של `main`
   (`git merge-base --is-ancestor origin/main HEAD`).
   אם לא - **למזג את `main` לתוך הענף של השיחה קודם כל**, לפני שמתחילים
   לעבוד, אחרת עלולים לבנות שוב על בסיס ישן.

**בסוף כל שיחה שסיימה פיצ'ר עובד ומאומת:** לדחוף לשני יעדים - (1) ענף
השיחה (הנדרש מהמערכת), (2) `main` (מקור האמת לקוד, ומשם קלאוד ראן
מתעדכן אוטומטית - ראה `.github/workflows/deploy-cloud-run.yml`). אם
המיזוג ל-`main` הוא fast-forward נקי (בדקו עם `git merge-base
--is-ancestor`), אפשר לדחוף ישירות. אם לא - למזג ידנית, לבדוק שכלום לא
נשבר (לרנדר עם Playwright, לא רק לקרוא קוד), ורק אז לדחוף. **אחרי
דחיפה ל-`main`, כדאי לוודא בפועל** שה-deployment לקלאוד ראן הצליח
(`mcp__github__actions_list` על `deploy-cloud-run.yml`, לבדוק
`conclusion: success` על הקומיט האחרון) לפני שאומרים למשתמש שזה מוכן.

**⚠️ עדכון 2026-09-19 - GitHub Pages הופסק:** בעבר הייתה כאן הוראה
ארוכה לדחוף גם לענף שממנו GitHub Pages מתעדכן (ענף נפרד מ-`main`,
משהו שגרם לתקלה אמיתית בעבר). **האבא אישר במפורש שאף אחד לא משתמש
יותר ב-GitHub Pages** - רק בקלאוד ראן. **אל תדחפו יותר לשום ענף מיוחד
של Pages**, ואל תשלחו את הקישור של `galdotan50.github.io` למישהו כ"קישור
למשחק" - הוא עלול להציג גרסה ישנה שקפאה בזמן הפסקת העדכונים.

**חשוב להסביר למשתמש (הוא ילד, לא מכיר גיט):** גם עם `main` קיים, **כל
שיחה חדשה עדיין תיפתח על ענף אקראי חדש** - זו התנהגות של המערכת שמריצה
את קלוד, לא משהו שקובץ בריפו יכול לשנות. הפתרון הוא לא "לבטל ענפים",
אלא לוודא (לפי הכללים למעלה) שכל שיחה **מתחילה** מסונכרנת עם `main`
ו**מסיימת** בדחיפה חזרה אליו - כך ש-`main` תמיד נשאר עדכני בפועל, גם אם
ה"תיקיות הזמניות" (ענפי ה-`claude/*`) ממשיכות להיווצר ולהצטבר ברקע.
ענפים ישנים שאין בהם שום דבר ש-`main` לא כבר מכיל אפשר למחוק מדי פעם -
אבל רק אחרי שוידאו את זה, ולא סתם כדי "לנקות".

לדחוף: `git push -u origin <branch>`. לא לפתוח PR אלא אם ביקשו במפורש.
